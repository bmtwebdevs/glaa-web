@model GLAA.ViewModels.LicenceApplication.AccommodatingWorkersViewModel

@{
    var text = "No";
    if (Model?.AccommodatesWorkers != null && Model.AccommodatesWorkers.Value)
    {
        text = $"Yes";
    }
}

@text