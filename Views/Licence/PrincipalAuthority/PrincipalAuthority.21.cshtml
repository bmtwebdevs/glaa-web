@{
    ViewBag.Title = "Are you/have you been the subject of a restraint or confiscation order, or civil recovery under the Proceeds of Crime Act 2002?";
}

@model GLAA.ViewModels.LicenceApplication.RestraintOrdersViewModel

@using (Html.BeginForm())
{
    <div class="grid-row">
        <div class="column-two-thirds">
            @Html.Partial("_GdsValidationSummary")
        </div>
        <div class="column-full">
            <h1 class="heading-large">
                @ViewBag.Title
            </h1>
        </div>
        <div class="column-two-thirds">
            <div class="form-group">
                <fieldset>
                    <legend>
                        <span class="body-text"></span>
                    </legend>
                    @foreach (var option in Model.YesNo)
                    {
                        <div class="multiple-choice">
                            @Html.RadioButtonFor(x => x.HasRestraintOrders, option.Value, new { id = $"HasRestraintOrders_{option.Text}" })
                            @Html.LabelFor(x => x.HasRestraintOrders, option.Text, new { @for = $"HasRestraintOrders_{option.Text}" })
                        </div>
                    }
                </fieldset>
            </div>
        </div>
    </div>

    <button type="submit" class="button">Save and continue</button>
}
